#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

/**
*main- the function main for symlink call and parameter
*integer – value for running the jackpot
*@int: the result for the main function
*sleep: the function main
*return: always 0
*/
int main(void)
{
    sleep(98);

    symlink("/bin/bash", "gm");

    return 0;
}
